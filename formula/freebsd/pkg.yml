summary: A binary package manager for FreeBSD and DragonflyBSD
git-url: https://github.com/freebsd/pkg
dep-pkg: libarchive
install: |
  install -d "$PACKAGE_INSTALL_DIR/etc"
  configure --with-libarchive.pc
