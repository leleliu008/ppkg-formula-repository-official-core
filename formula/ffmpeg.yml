summary: Play, record, convert, and stream audio and video
web-url: https://ffmpeg.org
src-url: https://ffmpeg.org/releases/ffmpeg-5.1.tar.xz
src-sha: 55eb6aab5ee235550fa54a33eaf8bf1b4ec66c01453182b12f6a993d75698b03
git-url: https://github.com/FFmpeg/FFmpeg.git
dep-pkg: sdl2 opus libwebp libvorbis theora speex x264 x265 lame
license: GPL-2.0-or-later

bsystem: gmake
binbstd: yes
install: |
    run ./configure \
        --prefix=$PACKAGE_INSTALL_DIR \
        --ar=$AR \
        --as=$AS \
        --ld=$CC \
        --cc=$CC \
        --cxx=$CXX \
        --nm=$NM \
        --ranlib=$RANLIB \
        --strip=$STRIP \
        --enable-static \
        --enable-shared \
        --enable-pic \
        --disable-asm \
        --disable-doc \
        --disable-debug &&
    gmakew clean &&
    gmakew install &&
    cp ffbuild/config.log $PACKAGE_INSTALLING_TMP_DIR
