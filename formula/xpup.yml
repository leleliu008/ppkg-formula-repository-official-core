summary: a command line tool for XML parsing
git-url: https://github.com/ericchiang/xpup.git
version: 0
license: Apache-2.0

install:
    bsystem: go
    actions: |
        run go mod init &&
        run go mod tidy &&
        gow
