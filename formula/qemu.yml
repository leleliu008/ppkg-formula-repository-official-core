summary: Emulator for x86 and PowerPC
web-url: https://www.qemu.org/
git-url: https://git.qemu.org/git/qemu
src-url: https://download.qemu.org/qemu-7.1.0.tar.xz
src-sha: a0634e536bded57cf38ec8a751adb124b89c776fe0846f21ab6c6728f1cbbbe6
license: GPL-2.0-only
dep-pkg: capstone libslirp libssh libusb libpng libjpeg-turbo pixman gnutls lzo nettle snappy vde libzstd libcap-ng xattr
dep-upp: ninja
binbstd: yes
install: |
    export CPPFLAGS="$(printf '%s\n' "$CPPFLAGS" | sed 's|-DNDEBUG||')"
    ./configure \
        --prefix=$PACKAGE_INSTALL_DIR \
        --cc=$CC \
        --host-cc=$CC \
        --extra-cflags="$CFLAGS $CPPFLAGS -DNCURSES_WIDECHAR=1" \
        --extra-ldflags="$LDFLAGS" \
        --disable-bsd-user \
        --disable-guest-agent \
        --enable-capstone \
        --enable-curses \
        --enable-libssh \
        --enable-slirp=system \
        --enable-vde \
        --enable-virtfs \
        --enable-zstd \
        --disable-sdl \
        --disable-gtk &&
    gmakew install V=1
