name: workflow8

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  ubuntu:
    if: ${{ false }}
    strategy:
      fail-fast: false
      matrix:
        tag: [18.04, 20.04, 22.04]
        pkg: [ttygif,ttyrec,tweego,uctags,unbound,unrar,unzip,uppm,utf8.h,utfcpp,vala,valgrind,vde,vegeta,vim,virustotal-cli,vsftpd,wabt,wasm3,wasmer,wasmtime,watchexec,webhook,websocketpp,wget,wolfssl,wuzz,xclip,xh,xmake,xmlto,xorriso,xpup,xvid,xxd,xz,yasm,youtubedr,yq,yyjson,zeromq,zip,zlib-ng,zlog,zola,zopfli,zoxide,zsh,zstd,zsync]

    runs-on:   ubuntu-latest
    container: ubuntu:${{ matrix.tag }}

    steps:
        - uses: actions/checkout@v3
          with:
            repository: leleliu008/ppkg-formula-repository-offical-core
            path: ppkg-formula-repository-offical-core

        - run: apt -y update
        - run: apt -y install curl xz-utils g++ gfortran linux-headers-generic

        - run: curl -LO https://raw.githubusercontent.com/leleliu008/ppkg/master/bin/ppkg
        - run: chmod a+x ppkg

        - run: ./ppkg setup

        - run: ./ppkg env

        - run: install -d /opt/ppkg/repos.d
        - run: mv ppkg-formula-repository-offical-core /opt/ppkg/repos.d/offical-core

        - run: ./ppkg install ${{ matrix.pkg }}

  macos:
    strategy:
      fail-fast: false
      matrix:
        tag: [12]
        pkg: [ttygif,ttyrec,tweego,uctags,unbound,unrar,unzip,uppm,utf8.h,utfcpp,vala,valgrind,vde,vegeta,vim,virustotal-cli,vsftpd,wabt,wasm3,wasmer,wasmtime,watchexec,webhook,websocketpp,wget,wolfssl,wuzz,xclip,xh,xmake,xmlto,xorriso,xpup,xvid,xxd,xz,yasm,youtubedr,yq,yyjson,zeromq,zip,zlib-ng,zlog,zola,zopfli,zoxide,zsh,zstd,zsync]

    runs-on: macos-${{ matrix.tag }}

    env:
        ACLOCAL_PATH: /usr/local/share/aclocal

    steps:
        - run: brew uninstall go@1.17

        - uses: actions/checkout@v3
          with:
            repository: leleliu008/ppkg-formula-repository-offical-core
            path: ppkg-formula-repository-offical-core

        - run: curl -LO https://raw.githubusercontent.com/leleliu008/ppkg/master/bin/ppkg
        - run: chmod a+x ppkg

        - run: ./ppkg setup --use-system-package-manager

        - run: ./ppkg env

        - run: install -d /opt/ppkg/repos.d
        - run: mv ppkg-formula-repository-offical-core /opt/ppkg/repos.d/offical-core

        - run: ./ppkg install ${{ matrix.pkg }}
